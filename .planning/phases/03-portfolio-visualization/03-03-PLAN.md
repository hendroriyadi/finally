---
phase: 03-portfolio-visualization
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - frontend/lib/useSseStream.ts
  - frontend/components/DetailChart.tsx
  - frontend/components/WatchlistRow.tsx
  - frontend/components/WatchlistPanel.tsx
  - frontend/app/page.tsx
autonomous: true
requirements: [UI-02]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Clicking a ticker row in the watchlist loads that ticker into the larger main detail chart"
    - "The detail chart keeps updating from the live SSE stream after selection, without a reload and without a new network connection"
    - "The detail chart's panel title names the currently selected ticker and changes on every new selection"
    - "The watchlist row currently driving the detail chart carries a persistent visual indicator that survives the mouse moving away"
    - "A watchlist row is operable by keyboard as well as by mouse, and using its remove control does not also select it"
    - "The detail chart shows more price history than the inline sparkline used to retain, because the one shared accumulator's cap was raised rather than a second accumulator being added"
    - "The default-selected ticker on first load is the first watchlist entry in seed order, and its accumulated price history renders as soon as points exist"
    - "If the watchlist is emptied entirely, the panel shows the no-ticker-selected prompt copy instead of a broken or empty chart"
    - "A just-selected ticker with fewer than two accumulated points renders the same flat-baseline placeholder treatment the sparkline already uses, at panel scale"
    - "Selecting among one or many watchlist tickers behaves identically — exactly one ticker is active at a time, with no multi-select state"
    - "Removing the ticker that was driving the detail chart moves the selection to another watchlist entry rather than leaving a stale or dead selection"
  artifacts:
    - path: "frontend/components/DetailChart.tsx"
      provides: "Full-width per-ticker price-history panel reading the shared SSE accumulator, with populated, partial, and no-selection states"
      min_lines: 80
      exports: ["DetailChart"]
    - path: "frontend/lib/useSseStream.ts"
      provides: "The one shared per-ticker price-history accumulator, with its retention cap raised to serve both the sparkline and the detail chart"
      min_lines: 110
      exports: ["usePriceStream", "MAX_SPARKLINE_POINTS", "PriceStreamState"]
  key_links:
    - from: "frontend/app/page.tsx"
      to: "frontend/components/WatchlistPanel.tsx"
      via: "passes the selected ticker down and receives selection changes back up"
      pattern: "onSelectTicker"
    - from: "frontend/components/WatchlistPanel.tsx"
      to: "frontend/components/WatchlistRow.tsx"
      via: "threads the per-row select callback and selected flag one level deeper, the same way removeControl is already threaded"
      pattern: "onSelect"
    - from: "frontend/components/DetailChart.tsx"
      to: "frontend/components/PriceStreamProvider.tsx"
      via: "reads the shared per-ticker history accumulator; opens no EventSource of its own"
      pattern: "usePriceStreamContext\\("
    - from: "frontend/app/page.tsx"
      to: "frontend/components/DetailChart.tsx"
      via: "the right column renders the detail chart above the heatmap and P&L row"
      pattern: "DetailChart"
---

<objective>
Make the watchlist interactive. Right now the grid is a display; after this plan a click on any row loads that ticker into a full-width chart that keeps drawing from the live stream, and the row itself shows which ticker the chart is answering for.

The chart's data does not come from a new endpoint. `usePriceStream` has been accumulating a per-ticker price series since page load for the sparklines this whole time — the detail chart is a second, larger reader of that same buffer. The only change to the accumulator is its retention cap: one constant, one buffer, two consumers.

Purpose: this is the phase's only new interaction, and it is what turns three static panels into a workstation a person drives.
Output: `components/DetailChart.tsx`, a clickable and keyboard-operable `WatchlistRow`, the lifted selection state in `app/page.tsx`, and a raised history cap in `lib/useSseStream.ts`.
</objective>

<execution_context>
@/Users/hendro/Documents/Projects/finally/.claude/gsd-core/workflows/execute-plan.md
@/Users/hendro/Documents/Projects/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-portfolio-visualization/03-CONTEXT.md
@.planning/phases/03-portfolio-visualization/03-RESEARCH.md
@.planning/phases/03-portfolio-visualization/03-UI-SPEC.md
@.planning/phases/03-portfolio-visualization/03-02-SUMMARY.md
@frontend/AGENTS.md
@frontend/lib/useSseStream.ts
@frontend/components/WatchlistPanel.tsx
@frontend/components/WatchlistRow.tsx
@frontend/components/Sparkline.tsx
@frontend/app/page.tsx

<interfaces>
<!-- Existing contracts this plan builds on. Use these directly; no exploration needed. -->

From `frontend/lib/useSseStream.ts` (Phase 1 — the accumulator being reused, not duplicated):
```typescript
export const MAX_SPARKLINE_POINTS = 60;          // this plan raises this one value
export interface PriceStreamState {
  status: ConnectionStatus;
  prices: PriceMap;
  history: Record<string, number[]>;             // per-ticker price series since page load, oldest first
  baselines: Record<string, number>;             // first price seen per ticker this session
}
export function usePriceStream(url: string): PriceStreamState;
```
`history[ticker]` is mutated in a ref and republished as a fresh shallow copy on every SSE frame, and
entries for tickers absent from a frame are deleted so a removed ticker does not leak. A ticker that
has received no frames yet has no key at all — read it as `history[ticker] ?? []`.

From `frontend/components/PriceStreamProvider.tsx` (Phase 1 — mounted in `app/layout.tsx`):
```typescript
export function usePriceStreamContext(): PriceStreamState;
```
Exactly one `EventSource` exists per page load, owned by this provider. No component opens its own.

From `frontend/components/WatchlistPanel.tsx` (Phases 1-2):
```typescript
export function WatchlistPanel(): JSX.Element;   // owns items + fetch-on-mount + add/remove callbacks
// renders <WatchlistRow ... removeControl={<RemoveTickerButton ticker={...} onRemoved={removeItem} />} />
```
`removeControl` is the existing precedent for threading a prop one level deeper into `WatchlistRow`;
the new selection props follow the same route rather than introducing a context.

From `frontend/components/WatchlistRow.tsx` (Phase 1):
```typescript
interface WatchlistRowProps { ticker: string; price?: number; changePercent?: number;
                              points: number[]; removeControl?: React.ReactNode }
```
Root element today:
`<div className="group flex h-9 items-center border-b border-edge px-2 hover:border-l-2 hover:border-l-accent hover:bg-panel">`

From `frontend/components/Sparkline.tsx` (Phase 1 — unchanged by this plan):
```typescript
export function Sparkline(props: { points: number[] }): JSX.Element;
// fewer than 2 points -> a flat centre-line placeholder at opacity 40
// otherwise -> a #209dd7 polyline, normalised with `const range = max - min || 1;`
```
</interfaces>
</context>

## Phase goal (verbatim from ROADMAP.md)

> A user can read their portfolio's shape and performance at a glance through a position heatmap, a value-over-time chart, and a per-ticker detail chart

This Goal line is not written in `As a / I want to / so that` user-story form. It is reproduced verbatim
rather than rewritten — run `/gsd mvp-phase 3` if a formal user story is wanted. This plan delivers the
per-ticker detail chart and the click interaction that drives it.

## Decisions implemented

IDs are assigned by this planner against `03-CONTEXT.md`'s prose bullets (same convention as Plans
03-01 and 03-02). CONTEXT.md's text is the authority; the ID is only a handle. D-18 comes from the
orchestrator's resolved open question, not from CONTEXT.md.

| ID | Decision (from `03-CONTEXT.md`, refined by the approved `03-UI-SPEC.md`) | Where |
|----|--------------------------------|-------|
| D-07 | The main detail chart uses Recharts too — one charting dependency for the whole app, not two | Task 1 |
| D-08 | The existing hand-rolled inline-SVG `Sparkline` is unchanged and is not replaced by a Recharts chart | Task 1 |
| D-15 | Clicking a ticker row in the watchlist grid selects it as the active ticker for a new, larger detail-chart panel; this needs new shared client state, since no "selected ticker" concept exists anywhere in the codebase yet | Task 1 |
| D-16 | The detail chart's price history is accumulated the same way the sparkline's is — from the existing SSE stream since page load, through `usePriceStream`'s existing per-ticker accumulator. Reuse it; do not duplicate it and do not add a server-side history endpoint for it | Task 1 |
| D-17 | The default selected ticker on first load is the first watchlist entry in seed order (`03-UI-SPEC.md` resolved CONTEXT.md's discretion point this way; treat it as locked). "No ticker selected" applies only if the watchlist is emptied entirely | Task 2 |
| D-18 | *(orchestrator-resolved open question)* Raise the shared `MAX_SPARKLINE_POINTS` cap from 60 to ~300 so one accumulator serves both the tiny sparkline and the full-panel detail chart. One constant, one buffer — explicitly not a second accumulator | Task 1 |

### Claude's-discretion choices made here

| Question (left open by `03-CONTEXT.md` / `03-RESEARCH.md`) | Choice | Rationale |
|---|---|---|
| Selected-ticker state: a new React context or lifted `useState` in `app/page.tsx` | Lifted `useState` in `app/page.tsx` | Its only two consumers, `WatchlistPanel` and `DetailChart`, are both direct children of `page.tsx`. The two existing contexts live in `layout.tsx` because their consumers span the layout/page boundary (`AppHeader` sits outside `{children}`); no such need exists here. `03-RESEARCH.md` A2 records the tradeoff: a third consumer later would justify promoting it, and promoting a `useState` to a context is a contained change |
| Keeping the constant's name `MAX_SPARKLINE_POINTS` after raising it | Keep the name | Nothing outside `useSseStream.ts` imports it, so a rename is cheap — but the resolved open question specifies a one-constant change, and the doc comment above it is updated to say the buffer now serves both consumers. Renaming would add churn to a file this plan otherwise barely touches |
| Detail chart X axis | Hidden, with an empty tooltip label | The accumulator stores prices only — no timestamps. Labelling the axis with a fabricated time or with a raw array index would assert precision the data does not carry. The Y axis (price) is the axis that means something, and it is fully labelled |
| Row interaction element | The existing root `<div>` given `role="button"`, `tabIndex`, and an Enter/Space key handler | The row already contains a remove `<button>`; nesting a button inside a button is invalid HTML. The remove control's wrapper stops click propagation so removing never also selects |

## Copy strings (verbatim from `03-UI-SPEC.md § Copywriting Contract`)

Use these exactly. Do not paraphrase, do not add a period, do not invent additional messages.

| Element | Copy |
|---|---|
| Detail chart panel title (populated) | `{TICKER} Price History` — dynamic; substitute the selected ticker |
| Detail chart no-selection heading | `No ticker selected` |
| Detail chart no-selection body | `Click a ticker in the watchlist to load its price history here.` |

There is no separate loading or error copy for this panel: it consumes an in-memory buffer, not a
request, so it can neither be mid-fetch nor fail. A selected ticker with too few points renders the
flat-baseline placeholder, which is a state, not an error.

## Colour, type, and layout tokens for this plan (from `03-UI-SPEC.md`)

| Element | Token |
|---|---|
| Panel shell | `rounded-md border border-edge bg-panel`, header `px-4 py-3` — identical to `PositionsTable` |
| Panel title | Heading scale: 20px, weight 600 (`text-xl font-semibold leading-tight`) |
| Line stroke | `#209dd7` (Primary), 2px, with a same-colour area fill at 10% opacity |
| Gridlines and axis lines | `#30363d`, 1px, solid — never dashed |
| Axis tick labels | Label scale: 12px, weight 600, muted `#8b949e` |
| Tooltip surface | background `#1a1a2e`, 1px border `#30363d`; value figure at Display scale with tabular numerals |
| Selected-row indicator | 2px left border in `#ecad0a` (`border-l-accent`) — the existing hover treatment, made sticky |
| Detail chart panel height | Taller than the two-up row beneath it — it has the most vertical room of the three new panels |
| Right-column stack gap | `lg` = 24px (`gap-6`), already in place from Plan 03-02 |

The selected-row indicator is the one place the accent colour appears in this plan, and it is the same
accent-on-watchlist-row usage Phase 1 already established — a sticky version of the existing hover
state, not a new accent surface.

## Artifacts this phase produces (Plan 03)

**New files:** `frontend/components/DetailChart.tsx`

**New symbols:**

| Symbol | Kind | Module |
|--------|------|--------|
| `DetailChart` | React client component, props `{ ticker: string \| null }` | `components/DetailChart` |

**Modified exports:**

| Symbol | Change |
|--------|--------|
| `MAX_SPARKLINE_POINTS` | value raised from `60` to `300`; name, module, and semantics unchanged |
| `WatchlistRowProps` | gains `selected: boolean` and `onSelect: () => void` |
| `WatchlistPanel` | gains props `{ selectedTicker: string \| null; onSelectTicker: (ticker: string \| null) => void }` |
| `Home` (`app/page.tsx`) | becomes a client component owning `selectedTicker` state and rendering `DetailChart` |

<!-- planner-discipline-allow: EventSource -->
<!-- planner-discipline-allow: hover:border-l-2 -->

<tasks>

<task type="auto">
  <name>Task 1: Click a ticker, watch its chart — selection state, an operable row, and the detail panel</name>
  <precondition>`recharts` is installed at the pinned version and `npm run build` succeeds with a Recharts component on the page (Plan 03-02), and `PriceStreamProvider` is mounted in `frontend/app/layout.tsx` (Phase 1).</precondition>
  <reversibility rating="reversible">Selection state is a single `useState` in `app/page.tsx` with two consumers; promoting it to a context later, if a third consumer appears, is a contained change confined to `frontend/`. No data format or persisted contract depends on it.</reversibility>
  <files>frontend/lib/useSseStream.ts, frontend/components/DetailChart.tsx, frontend/components/WatchlistRow.tsx, frontend/components/WatchlistPanel.tsx, frontend/app/page.tsx</files>
  <read_first>
    - `frontend/AGENTS.md` — this Next.js version has breaking changes from training data; read the relevant guide under `node_modules/next/dist/docs/` before writing App Router code
    - `frontend/lib/useSseStream.ts` in full — especially the module doc comment explaining why accumulation lives in refs and is republished as a shallow copy, and the retention/`splice` block being re-tuned
    - `frontend/components/WatchlistRow.tsx` in full — the existing root element, the flash effect, and the fixed column widths that must not shift when the selected state is added
    - `frontend/components/WatchlistPanel.tsx` in full — how `removeControl` is threaded into each row, and the branch ordering of the grid's states
    - `frontend/components/Sparkline.tsx` in full — the fewer-than-two-points flat-baseline placeholder this panel reproduces at a larger scale, and the `|| 1` range guard
    - `frontend/components/PnLChart.tsx` as Plan 03-02 leaves it — the Recharts composition, the panel shell, and the tick/tooltip styling to stay consistent with
    - `frontend/app/page.tsx` as Plan 03-02 leaves it — the two-column grid and the right column this panel goes on top of
    - `.planning/phases/03-portfolio-visualization/03-RESEARCH.md` sections "Pattern 3" and "Pitfall 5"
  </read_first>
  <behavior>
    - Clicking any watchlist row loads that ticker into the detail panel and the panel title names it
    - Clicking a different row switches the panel to that ticker; exactly one row is ever active
    - The active row keeps a visible left-edge indicator after the pointer moves away
    - Tabbing to a row and pressing Enter or Space selects it, the same as a click
    - Clicking a row's remove control removes the ticker and does not also select that row
    - The chart keeps drawing new points as the stream ticks, with no new network connection opened
    - A ticker selected before it has received two stream frames renders a flat baseline placeholder, then becomes a line once points accumulate
    - The chart retains noticeably more history than the inline sparkline previously did, from the same buffer
    - Row column widths and row height are identical whether a row is selected or not
  </behavior>
  <action>
    Implements D-07, D-08, D-15, D-16, D-18.

    **`frontend/lib/useSseStream.ts`** — change the value of `MAX_SPARKLINE_POINTS` from `60` to `300`
    and update the doc comment above it. Change nothing else in this file: the accumulator, the
    truncating `splice`, the per-frame republish, and the removed-ticker cleanup all stay exactly as
    they are. Say in the comment that this one buffer now feeds two consumers — the 60x20px inline
    sparkline and the full-panel detail chart — and that 300 points at the stream's roughly 500ms
    cadence is about two and a half minutes of history, still a hard cap so a long-lived tab cannot grow
    without bound. Do not add a second accumulator, a per-consumer cap, or a parallel history hook:
    duplicating the buffer is exactly what D-16 forbids, and two buffers would drift apart on the
    removed-ticker cleanup path.

    **`frontend/components/DetailChart.tsx`** — new component, `"use client"` on the first line (every
    Recharts component needs browser APIs and this project's static export prerenders pages at build
    time, so a missing directive fails the build). Props: `{ ticker: string | null }`. Read
    `const { history } = usePriceStreamContext()` and derive `const points = ticker ? history[ticker] ?? [] : []`.
    It opens no stream connection and issues no request of its own — the provider owns the one
    connection to the server, and this panel is simply its second reader.

    Render the panel shell used by `PositionsTable` and `PnLChart`, with the title
    `` `${ticker} Price History` `` at the heading scale when a ticker is selected. Branch: no ticker
    selected renders the no-selection heading and body copy (Task 2 makes this state actually reachable);
    fewer than two points renders a flat baseline placeholder — a full-width horizontal rule at the
    vertical centre of the chart area at 40% opacity, the same treatment `Sparkline` gives this exact
    case, scaled up; otherwise the chart.

    For the chart, map the numeric array into `points.map((price, index) => ({ index, price }))` and
    wrap `<ResponsiveContainer width="100%" height={320}>` around an `<AreaChart>` with
    `margin={{ top: 8, right: 16, bottom: 8, left: 0 }}`. Height 320 is deliberately taller than the
    220 the P&L chart uses: the UI-SPEC gives this panel the most vertical room of the three because a
    price-over-time series reads better tall, and it spans the full right-column width above the two-up
    row. Include `<CartesianGrid stroke="#30363d" strokeDasharray="0" vertical={false} />`,
    `<XAxis dataKey="index" hide />`, and a visible
    `<YAxis stroke="#8b949e" fontSize={12} tickLine={false} domain={["auto", "auto"]} />` with a
    currency `tickFormatter` in the same local-helper style `PnLChart` and `PositionsTable` use. Hide
    the X axis deliberately: the accumulator carries prices with no timestamps, so any x-label would be
    either a fabricated time or a meaningless array index, and `domain={["auto", "auto"]}` on Y is what
    keeps a stock oscillating within a dollar from rendering as a flat line pinned to zero. Add
    `<Tooltip contentStyle={{ backgroundColor: "#1a1a2e", border: "1px solid #30363d" }} labelFormatter={() => ""} />`
    so hovering reports the price without captioning it with an index. Draw
    `<Area type="monotone" dataKey="price" stroke="#209dd7" strokeWidth={2} fill="#209dd7" fillOpacity={0.1} dot={false} isAnimationActive={false} />`.
    `isAnimationActive={false}` matters more here than anywhere else in the phase: this component
    re-renders about twice a second off the stream, and an animated redraw each time would be a
    permanently-animating chart.

    **`frontend/components/WatchlistRow.tsx`** — add `selected: boolean` and `onSelect: () => void` to
    the props interface and make the root element operable. Give the root `role="button"`,
    `tabIndex={0}`, `aria-pressed={selected}`, `onClick={onSelect}`, `cursor-pointer`, and an
    `onKeyDown` that calls `event.preventDefault()` then `onSelect()` when `event.key` is `"Enter"` or
    `" "`. Keep it a `<div>` rather than promoting it to a `<button>`: the row already contains the
    remove `<button>`, and a button inside a button is invalid HTML that browsers resolve
    unpredictably.

    Rework the root's border classes so selection does not move anything. Today the row draws its left
    accent edge only under a hover variant that also sets the border width, so content jumps 2px the
    moment the pointer lands on a row — see the root element quoted in this plan's `<interfaces>` block
    for the current class list. Make the left border permanent and only change its colour: a
    constant `border-l-2` with
    `border-l-transparent` by default, `hover:border-l-accent` retained, and `border-l-accent` applied
    unconditionally when `selected` is true. That gives the sticky indicator the UI-SPEC asks for — the
    same accent treatment as hover, but persistent so the user can see which ticker the chart is
    answering for after the mouse moves away — and removes the pre-existing hover jitter as a side
    effect rather than adding a new one.

    Wrap the `removeControl` slot's container so a click on it stops there:
    add `onClick={(event) => event.stopPropagation()}` (and the same on `onKeyDown`) to the
    `<div className="flex w-[60px] items-center justify-end">` that holds it. Without this, removing a
    ticker would also select the row that is about to disappear. `RemoveTickerButton` itself needs no
    change.

    **`frontend/components/WatchlistPanel.tsx`** — add props
    `{ selectedTicker: string | null; onSelectTicker: (ticker: string | null) => void }` and thread them
    into each row as `selected={item.ticker === selectedTicker}` and
    `onSelect={() => onSelectTicker(item.ticker)}`, alongside the existing `removeControl` prop. This is
    the same one-level-deeper prop threading `removeControl` already established, which is why no
    context is introduced. Leave the fetch, the add/remove callbacks, and every state branch otherwise
    untouched in this task.

    **`frontend/app/page.tsx`** — add `"use client"` at the top (it now owns state) and
    `const [selectedTicker, setSelectedTicker] = useState<string | null>(null)`. Pass
    `selectedTicker={selectedTicker}` and `onSelectTicker={setSelectedTicker}` to `WatchlistPanel`, and
    render `<DetailChart ticker={selectedTicker} />` as the first child of the right column, above the
    heatmap/P&L two-up row Plan 03-02 created. The default value stays `null` in this task; Task 2 makes
    it resolve to the first watchlist entry.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; grep -q 'MAX_SPARKLINE_POINTS = 300' lib/useSseStream.ts &amp;&amp; grep -q 'use client' components/DetailChart.tsx &amp;&amp; grep -q 'usePriceStreamContext' components/DetailChart.tsx &amp;&amp; grep -q 'selectedTicker' app/page.tsx &amp;&amp; grep -q 'DetailChart' app/page.tsx &amp;&amp; grep -q 'onSelectTicker' components/WatchlistPanel.tsx &amp;&amp; grep -q 'aria-pressed' components/WatchlistRow.tsx &amp;&amp; grep -q 'stopPropagation' components/WatchlistRow.tsx &amp;&amp; test "$(grep -vE '^\s*(//|\*|/\*)' components/DetailChart.tsx | grep -cE 'EventSource|fetch\(')" = "0" &amp;&amp; test "$(grep -c 'hover:border-l-2' components/WatchlistRow.tsx)" = "0"</automated>
    <human-check>With the app open, click three different watchlist rows in turn: the detail panel title changes to each ticker, the chart redraws for it, and the clicked row keeps a yellow left edge after the mouse moves away. Tab to a row and press Enter — the same selection happens. Click a row's remove control — the ticker disappears and the detail panel does not switch to it. Watch a selected chart for a minute and confirm it keeps extending rather than resetting, and that it holds visibly more history than a watchlist sparkline shows.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -q 'MAX_SPARKLINE_POINTS = 300' frontend/lib/useSseStream.ts` succeeds, and the file still declares exactly one history accumulator ref
    - `frontend/components/Sparkline.tsx` is byte-identical to its pre-task state (D-08)
    - `grep -vE '^\s*(//|\*|/\*)' frontend/components/DetailChart.tsx | grep -cE 'EventSource|fetch\('` returns 0 — the panel reads the shared accumulator and opens nothing of its own (D-16)
    - `frontend/components/DetailChart.tsx` begins with the `"use client"` directive and accepts a `ticker: string | null` prop
    - The chart branch for fewer than two points renders a flat baseline placeholder, not an empty chart frame and not the no-selection copy
    - `WatchlistRow`'s root element carries `role="button"`, `tabIndex`, `aria-pressed`, an `onClick`, and a keyboard handler covering Enter and Space
    - `grep -c 'hover:border-l-2' frontend/components/WatchlistRow.tsx` returns 0 — the left border is permanent and only its colour changes, so selection and hover cause no layout shift
    - The remove-control container stops click propagation, so removal never selects
    - `frontend/app/page.tsx` owns a single `selectedTicker` state value and passes it to both `WatchlistPanel` and `DetailChart`; no new React context is created
    - `cd frontend && npm run lint` exits 0 and `npm run build` completes the static export without a prerender error
  </acceptance_criteria>
  <done>A user clicks a ticker in the watchlist and its price history fills a full-width chart that keeps drawing from the stream, with the clicked row visibly marked as the one the chart is answering for.</done>
</task>

<task type="auto">
  <name>Task 2: A selection that is never wrong — default it, follow removals, and say so when there is nothing to show</name>
  <files>frontend/components/WatchlistPanel.tsx, frontend/components/DetailChart.tsx</files>
  <read_first>
    - `frontend/components/WatchlistPanel.tsx` as Task 1 leaves it — the fetch-on-mount `.then()` chain, `addItem`, and `removeItem`
    - `frontend/components/DetailChart.tsx` as Task 1 leaves it — the branch ordering the no-selection state slots into
    - `.planning/phases/03-portfolio-visualization/03-UI-SPEC.md` sections "Copywriting Contract" and "UI Considerations" — specifically the `detail-chart` empty row and the note that "no ticker selected" is only reachable by emptying the watchlist
    - `frontend/components/PortfolioProvider.tsx` lines 88-131 — the comment explaining why an effect's dependency list must not include a value whose identity changes every SSE frame; the same discipline applies to any effect added here
  </read_first>
  <behavior>
    - On first load with the seeded watchlist, the detail chart is already showing the first ticker before the user clicks anything
    - The default is applied once; a later manual selection is never overwritten by a watchlist refresh or by adding a ticker
    - Removing the currently-selected ticker moves the selection to another remaining watchlist entry rather than leaving the chart pointed at a ticker no longer listed
    - Removing a non-selected ticker leaves the selection alone
    - Removing the last remaining ticker clears the selection and the panel shows the no-ticker-selected copy
    - Adding a ticker to a non-empty watchlist does not change which ticker the chart is showing
    - With the watchlist still loading or failed, no default is applied and the panel shows the no-selection prompt rather than a chart for a ticker that may not exist
  </behavior>
  <action>
    Implements D-17, and completes the no-selection state D-15 introduced.

    **`frontend/components/WatchlistPanel.tsx`** — apply the default selection from inside the existing
    fetch-on-mount `.then()` chain, not from a new effect. Add a
    `const defaultSelectionAppliedRef = useRef(false)` and, in the `.then()` callback that already calls
    `setItems(tickers)`, additionally call `onSelectTicker(tickers[0].ticker)` when the ref is still
    false and `tickers.length > 0`, then set the ref to true. Guard on the ref, never on a comparison
    against the currently selected value: reading the live selection here would either need it in a
    dependency list (re-running the default whenever the user changes selection, fighting them for
    control) or read a stale closure value. The ref makes the intent literal — apply a default exactly once per mount,
    and never again. The first entry is what the server returns first, which for a seeded database is
    the seed order the UI-SPEC specifies (D-17). Do not apply a default when the fetch fails or returns
    an empty list: selecting a ticker that is not listed would point the chart at something the user
    cannot see or deselect.

    In `removeItem`, after filtering the removed ticker out of `items`, reconcile the selection: if the
    removed ticker is the selected one, call `onSelectTicker` with the first ticker remaining after the
    removal, or `null` when none remain. Compute the remaining list from the same filtered array the
    state update produces so the two cannot disagree, and leave the selection untouched when a
    non-selected ticker is removed. Without this, removing the selected ticker leaves the chart titled
    for a ticker that is gone from the grid, and — because `usePriceStream` deletes history for tickers
    absent from a frame — its series would empty out underneath it, which reads as a broken chart rather
    than as a removed ticker.

    `addItem` needs no selection change: a newly added ticker does not steal focus from whatever the
    user is currently looking at, and the UI-SPEC describes exactly one active selection at a time with
    no multi-select state.

    **`frontend/components/DetailChart.tsx`** — confirm and finish the no-selection branch Task 1
    stubbed. When `ticker` is `null`, render the panel shell with the no-selection heading at the
    heading scale and the body copy at the body scale in muted `#8b949e`, using the exact Copywriting
    Contract strings, in place of the chart and in place of a dynamic title. Do not render a title
    naming a null ticker, and do not render an empty chart frame. This state is only reachable once the
    watchlist has been emptied entirely, which is exactly what the copy tells the user how to fix.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; grep -q 'defaultSelectionAppliedRef' components/WatchlistPanel.tsx &amp;&amp; grep -q 'onSelectTicker' components/WatchlistPanel.tsx &amp;&amp; grep -q 'No ticker selected' components/DetailChart.tsx &amp;&amp; grep -q 'Click a ticker in the watchlist to load its price history here.' components/DetailChart.tsx &amp;&amp; test "$(grep -vE '^\s*(//|\*|/\*)' components/WatchlistPanel.tsx | grep -c 'selectedTicker === null')" = "0"</automated>
    <human-check>Load the app fresh: the detail panel is already showing the first seeded ticker (AAPL) with no click. Select a different ticker, then add a new ticker via the form — the selection stays where you put it. Remove the selected ticker — the chart moves to another listed ticker, not to a blank or stale one. Remove every ticker — the panel shows "No ticker selected" with its prompt copy. Add one back and click it — the chart returns.</human-check>
  </verify>
  <acceptance_criteria>
    - The default selection is applied from inside the existing watchlist fetch `.then()` chain, guarded by a ref, and `grep -vE '^\s*(//|\*|/\*)' frontend/components/WatchlistPanel.tsx | grep -c 'selectedTicker === null'` returns 0 — the guard is the ref, not a read of the current selection
    - No default is applied when the watchlist fetch fails or returns an empty list
    - `removeItem` reassigns the selection only when the removed ticker is the selected one, choosing the first remaining entry or `null` when the list is emptied
    - `addItem` contains no call that changes the selection
    - `frontend/components/DetailChart.tsx` renders the no-selection heading and body copy verbatim and renders no title when `ticker` is null
    - No new `useEffect` is introduced in `WatchlistPanel.tsx` by this task
    - `cd frontend && npm run lint` exits 0 and `npm run build` completes the static export without a prerender error
  </acceptance_criteria>
  <done>The detail chart is showing something useful from the moment the page loads, follows the watchlist when a ticker is removed, and says plainly what to do in the one case where there is genuinely nothing to show.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE frames → in-memory per-ticker history buffer | An unauthenticated server-pushed stream determines how much browser memory the tab retains |
| user click / keypress → selected ticker string | A user-chosen string becomes a key into the price and history maps |
| selected ticker → rendered panel title | A ticker string is interpolated into visible text |
| watchlist mutation → selection state | A removal can leave a reference pointing at something that no longer exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-16 | Denial of Service | per-ticker history retention after raising the cap | medium | mitigate | The cap stays a hard cap and the same truncating `splice` runs on every frame — only its value changes, from 60 to 300. At this app's scale (a 50-ticker watchlist ceiling enforced server-side by `MAX_WATCHLIST_SIZE`) worst-case retention is 15,000 numbers, and the existing removed-ticker cleanup still frees entries for tickers that leave the stream. No unbounded path is introduced. |
| T-03-17 | Tampering | selected ticker used as a key into `history` and `prices` | low | mitigate | Selection can only be set from a ticker already present in the fetched watchlist, and every read is `history[ticker] ?? []`, so a missing key resolves to the designed partial state rather than an unguarded property access. |
| T-03-18 | Information Disclosure | ticker string interpolated into the panel title | low | mitigate | The value originates from the watchlist API, which the server already constrains through `TICKER_PATTERN` and a 10-character bound, and React escapes interpolated text by default — no `dangerouslySetInnerHTML` anywhere in this plan. |
| T-03-19 | Availability | selection left pointing at a removed ticker | medium | mitigate | `removeItem` reconciles the selection to the first remaining entry, or to `null` when the list empties, so the chart can never be titled for a ticker absent from the grid while its history is being garbage-collected by the stream hook. |
| T-03-20 | Availability | remove control also firing row selection | low | mitigate | The remove-control container stops click and key propagation, so the destructive action and the selection action cannot both fire from one interaction. |
| T-03-21 | Denial of Service | continuous chart animation at stream cadence | low | mitigate | `isAnimationActive={false}` on the detail chart's `<Area>`. The component re-renders about twice a second off the SSE stream; leaving animation on would keep a full-panel chart permanently mid-transition and burn frame budget for no information gain. |
| T-03-22 | Tampering | a second, divergent history accumulator | medium | mitigate | This plan adds no accumulator. An acceptance criterion asserts `useSseStream.ts` still declares exactly one history ref, and a grep asserts `DetailChart.tsx` opens no `EventSource` and issues no fetch — two buffers would drift apart on the removed-ticker cleanup path and produce a sparkline and a chart that disagree for the same ticker. |
| T-03-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs zero new packages. `recharts` was installed and human-verified under Plan 03-02's blocking legitimacy checkpoint (`T-03-SC` there); no install task exists here to gate. |
</threat_model>

<verification>
1. `cd frontend && npm run lint` — clean
2. `cd frontend && npm run build` — the static export completes; no prerender error from `DetailChart`
3. `grep -n 'MAX_SPARKLINE_POINTS' frontend/lib/useSseStream.ts` — one declaration, value 300, still used by the truncating `splice`
4. `git diff --stat frontend/components/Sparkline.tsx` — no changes
5. With the app running: fresh load shows the first seeded ticker in the detail panel; clicking three different rows switches the chart and moves the sticky left-edge indicator; keyboard Tab + Enter selects; the remove control removes without selecting
6. Remove the selected ticker and confirm the chart moves to a remaining one; remove all tickers and confirm the no-ticker-selected copy appears
7. Leave a selected chart open for two minutes and confirm it accumulates rather than resetting, and holds visibly more history than the row sparkline
</verification>

<success_criteria>
- Clicking a ticker in the watchlist loads it into the main detail chart, which keeps updating from the live stream (UI-02)
- The selected row carries a persistent indicator, and rows are operable by both mouse and keyboard (UI-02)
- The detail chart reads the one existing per-ticker accumulator; no second buffer, no new endpoint, no second `EventSource` (UI-02)
- The shared retention cap is raised in place so both the sparkline and the detail chart read a longer series from the same buffer
- The first watchlist entry is selected by default on load, removals reconcile the selection, and the emptied-watchlist case shows the specified prompt copy
- The existing `Sparkline` component is untouched
</success_criteria>

<output>
Create `.planning/phases/03-portfolio-visualization/03-03-SUMMARY.md` when done
</output>
